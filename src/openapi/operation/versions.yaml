get:
  operationId: getVersion
  description: "Get application version information"
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: 'model/VersionResponse.json'
    304:
      description: Not modified